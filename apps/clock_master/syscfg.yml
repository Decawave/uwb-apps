syscfg.vals:
    # Route console to RTT
    CONSOLE_UART: 0
    CONSOLE_RTT: 1
    DW1000_DEVICE_0: 1

syscfg.defs:
    DEVICE_ID:
        description: >
            Device ID
        value: ((uint16_t){0x8000})
    DEVICE_UUID:
        description: >
            UUID
        value: ((uint64_t){0x8000})    